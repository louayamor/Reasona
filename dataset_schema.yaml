dataset:
  name: codelion/synth-1B
  source: huggingface

columns:
  synth_id:
    type: string
    role: id

  language:
    type: string
    role: metadata

  exercise:
    type: string
    role: metadata

  text:
    type: string
    role: content

target:
  name: text

indexing:
  text_fields:
    - text

  metadata_fields:
    - synth_id
    - language
    - exercise
